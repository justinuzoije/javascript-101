// function cipher(phrase, offset) {
//
//   theOffset = Number(offset);
//   index = "abcdefghijklmnopqrstuvwxyz";   ///do not name arrays index in Javascript! it changes them
//   cipherIndex = "";
//   cipherPhrase = "";
//
//   for (var i = 0; i < index.length; i++) {
//
//   }
//   for (var j = 0; j < phrase.length; j++) {
//     cipherPhrase = cipherPhrase + (phrase[i] +
//   }
//
// }
//
//



// //Example
// function caesarShift(text, shift) {
// 	var result = "";
// 	for (var i = 0; i < text.length; i++) {
// 		var c = text.charCodeAt(i);
// 		if      (c >= 65 && c <=  90) result += String.fromCharCode((c - 65 + shift) % 26 + 65);  // Uppercase
// 		else if (c >= 97 && c <= 122) result += String.fromCharCode((c - 97 + shift) % 26 + 97);  // Lowercase
// 		else                          result += text.charAt(i);  // Copy
// 	}
// 	return result;
// }
